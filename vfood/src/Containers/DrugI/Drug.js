import React from 'react';
import Card from '../../Components/Card/Card';
import Back from '../../Components/BackButton/Back';

const Drug = () => {
  const cardStyle = {
    margin: '100px',
  };
   <div id='hello'>
   </div>
  return (
    <div>
      <Back />
      <Card height={31} style={cardStyle}>
        Ask a question 
      </Card>
    </div>
  );
}; 

export default Drug;
